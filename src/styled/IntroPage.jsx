import styled from "styled-components";

const IntroPage = styled.div`
slide {
  }

  .slide img {
    max-width: 100%;
    min-height: 650px; 
    /* 다른 이미지 관련 스타일을 추가할 수 있습니다. */
  }
`;

export default IntroPage;